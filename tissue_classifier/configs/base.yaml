defaults:
  - hydra: default
  - logger: mlflow
  - model: default
  - trainer: default
  - _self_

seed: 42
mode: fit        # fit | validate | test | predict
checkpoint: null # path or mlflow-artifacts:// URI; used as ckpt_path when mode != fit

thumbnail_size: [512, 512]

data:
  batch_size: ${training.batch_size}
  num_workers: ${training.num_workers}
  # train / val / test dataset configs are merged from data/splits: mmci

metadata:
  experiment_name: RI Scale
  run_name: ???
  description: "Binary LN vs colorectum classification"
