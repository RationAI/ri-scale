# @package _global_

metadata:
  run_name: Dataset Split
  description: Split slides into train folds / val / test respecting patient grouping
  hyperparams:
    seed: ${seed}
    val_fraction: ${val_fraction}
    test_fraction: ${test_fraction}
    n_folds: ${n_folds}
